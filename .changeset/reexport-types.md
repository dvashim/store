---
"@dvashim/store": patch
---

Narrow public type exports to `Selector` and `Subscriber` only, keeping `SourceStore` and `UpdateOptions` as internal types

- Remove commented-out example code from `ComputedStore`
- Update README and CLAUDE.md to reflect the changed type exports
